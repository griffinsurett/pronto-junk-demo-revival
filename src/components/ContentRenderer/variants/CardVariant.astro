---
// src/components/ContentRenderer/variants/CardVariant.astro
/**
 * Card Variant - Feature showcase with prominent cards
 * Features: Large cards, hover effects, images, 1-4 columns
 */

import type { BaseVariantProps } from "../ContentRenderer.types";
import FeatureCard from "@/components/LoopComponents/FeatureCard.astro";
import Button from "@/components/Button/Button";
import { shouldShowCollectionCTA } from "../utils/helpers";
import { animationProps, staggeredAnimationProps } from "@/integrations/scroll-animations";

interface Props extends BaseVariantProps {
  columns?: 1 | 2 | 3 | 4;
}

const {
  items = [],
  title,
  description,
  className = "",
  columns = 3,
  collectionUrl,
  collectionTitle,
  id,
} = Astro.props as Props;

const viewAllText = `Explore All ${collectionTitle || ""}`.trim();

const columnClasses = {
  1: "grid-cols-1",
  2: "grid-cols-1 md:grid-cols-2",
  3: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3",
  4: "grid-cols-1 md:grid-cols-2 lg:grid-cols-4",
};
---

<section id={id} class={`py-16 ${className}`}>
  {
    (title || description) && (
      <div {...animationProps("fade-in-up", { once: true })} class="text-center mb-12">
        {title && <h2 class="text-4xl text-heading font-bold mb-4">{title}</h2>}
        {description && (
          <p class="text-lg text-text max-w-2xl mx-auto">{description}</p>
        )}
      </div>
    )
  }

  {
    items.length > 0 && (
      <ul class={`grid ${columnClasses[columns]} gap-8 list-none items-stretch`}>
        {items.map((item, i) => (
          <li {...staggeredAnimationProps("fade-in-up", i, { once: true, staggerDelay: 100 })} class="flex">
            <FeatureCard {...item} />
          </li>
        ))}
      </ul>
    )
  }

  {
    shouldShowCollectionCTA(collectionUrl, items.length) && (
      <div {...animationProps("pop-in", { once: true })} class="mt-12 text-center">
        <Button
          href={collectionUrl}
          rightIcon="lu:chevron-right"
          variant="secondary"
          size="lg"
        >
          {viewAllText}
        </Button>
      </div>
    )
  }
</section>
